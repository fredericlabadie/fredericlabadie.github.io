# Resume / CV

Drop the PDF in this folder using the exact filename:

```
frederic-labadie-cv.pdf
```

The site's "Download CV" link points at `/assets/resume/frederic-labadie-cv.pdf`.

If you need a different filename, update the `href` in two places:

- `index.html` &mdash; hero actions and contact-list
- `philips.html` &mdash; hero actions (if added there too)

Keep the file size reasonable (under 1&nbsp;MB).
